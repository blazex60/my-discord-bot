# P0 automated verification

- `npm ci`; cwd `music-bot/`; exit `0`; assertion: 320 packages installed from the lockfile and the package/lock contract was accepted; artifact: terminal receipt summarized here; cleanup: npm left only ignored `node_modules/`; resources: `port=none,database=none,browserProfile=none,composeProject=none`.
- `node -e "const Database=require('better-sqlite3'); ..."`; cwd `music-bot/`; exit `0`; assertion: an in-memory database returned `{ ok: 1 }`, closed, and printed `P0_SQLITE_OPEN_OK`; artifact: this receipt; cleanup: in-memory DB closed; resources: `database=:memory:`, all other tuple fields `none`.
- `npm ls --depth=0`; cwd `music-bot/`; exit `0`; assertion: all declared runtime/dev dependencies resolved without invalid or missing packages; artifact: `dependency-inventory.json`; cleanup: observation only; resources: all tuple fields `none`.
- `npm run check`; cwd `music-bot/`; exit `0`; assertion: 53 server/harness tests passed, one Vitest DOM test passed, typecheck printed `P0_TYPECHECK_OK`, Vite printed `P0_VITE_BUILD_OK`, and real Chromium printed `1 passed` plus `P0_CHROMIUM_RUN_OK`; artifacts: `happy-*.log` and this receipt; cleanup: wrappers removed temporary Vite and Playwright roots; resources: `port=none,database=temporary-memory,browserProfile=temporary,composeProject=none`.
- Disposable full-manifest runs after review hardening printed `P0_FILTERED_ENV_HAPPY_OK` and `P0_ATOMIC_HAPPY_OK`; cwd `music-bot/`; exit `0`; assertion: every P0 manifest step works with the child-env allowlist and exclusive no-follow evidence writes; cleanup: each in-project `.qa-temp-*` sandbox was removed in `finally`.
- `git diff --check -- music-bot`; cwd repository root; exit `0`; assertion: no whitespace errors; artifact: this receipt; cleanup: observation only; resources: all tuple fields `none`.
- `npm audit` as part of install reported 10 transitive findings (`4 moderate`, `6 high`); no forced dependency changes were made because P0 is pinned to the approved dependency ranges and unrelated remediation is outside scope.
