discord:
  command_prefix: "/"

llm:
  url: "http://llm:8080"
  timeout_seconds: 120
  max_tokens: 2048
  n_ctx: 8192

storage:
  db_path: "data/messages.db"
  ttl_days: 30
  max_messages_per_query: 80

monitoring:
  excluded_channels: []

autopost:
  enabled: false
  default_channel: null
