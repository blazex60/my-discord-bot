{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [[ \"$f\" == *.py ]] && uv run ruff format \"$f\"; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "ruff format 実行中..."
          }
        ]
      }
    ]
  }
}
